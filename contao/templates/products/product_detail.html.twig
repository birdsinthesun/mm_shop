  {% for item in items %}
        <div class="product-detail">
            <div class="product-gallery">
            {{ item.html5.images|raw }}
            </div>
           <div class="product-info">
           <h2> {{ item.html5.name|raw }}</h2>
            <h3>{{ item.html5.brand|raw }}</h3>
            {{ item.html5.description|raw }}
            <span class="price">
            {{ item.html5.price|raw }} €
                  </span>  
            {{ item.html5.tax|raw }} 
              
            </div>
           <div class="product-buttons">
               
                    {% set product_id = item.raw.id %}
                    <a href='{{ url }}?add={{ product_id }}' title='Produkt in den Warenkorb legen'>In den Warenkorb</a>
                      
           </div> 
        </div>
    {% endfor %}